MASTER PROMPT — Exploding Box Configurator (2026)

The full project source code is available here on GitHub:
https://github.com/mpucko-pixel/exploding_box

Please load the repository and confirm you have access to all project files.

This prompt restores the complete development context for the Exploding Box configurator.

GOAL:
Build a production‑quality 3D product configurator supporting:
- texture uploads
- template system
- lid/top design
- figurines
- PNG/GLB export
- ordering pipeline

TECH:
React • R3F • Three.js • Zustand • React Spring • Vite 8 • GLTFExporter

ARCHITECTURE:
Follow the exact folder structure:
```
src/
  box/    (lid, base, paper, figurines, decor)
  core/   (animation, lighting, materials, utils)
  export/ (png, glb, background)
  ui/     (panels, controls, widgets, layout)
  state/  (store, selectors)
```

KEY SYSTEMS:
- BoxAnimator: sequential animation
- PaperInset: tileBase/fit/tileSquare
- textureCache: SRGB caching
- FixedLights + presets
- Materials: cardboard + paper
- Zustand store: textures/papers/figurine/config
- Export system: PNG supersampling, GLB export

RULES:
- Maintain folder structure
- Keep imports correct
- Preserve PaperInset + BoxAnimator behavior
- Keep UI and Scene decoupled
- Provide full code blocks when updating

FUTURE WORK:
- Template loader
- Export UI
- Order JSON
- Render modes

END MASTER PROMPT
